import { ImageType } from '@/app/protocols'

export interface LinkedinValues {
  id: string
  title: string
  description: string
  buttonText: string
  arrow: ImageType
  linkLinkedIn: string
  image: ImageType
}
